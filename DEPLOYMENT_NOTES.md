Deployment Notes

This document records deployment experiments performed for the
StayOnTrack project using Render and Supabase.

The project was successfully built and dependencies resolved,
but full production deployment was intentionally paused due to
model compatibility considerations.

This repository is currently maintained as a prototype /
learning project.

A production-ready deployment will be implemented in a future
version of StayOnTrack or a separate project.
